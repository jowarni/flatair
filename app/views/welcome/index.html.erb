<div id="accueil">
  <h1>Rent your flat</h1>
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-12">
          <%@flats.each do |flat|%>
          <div class="card">
          <h4><%=flat.title%></h4>
          <p>Price: <%=flat.price%>€</p>
          <p>City: <%=flat.city%></p>
          <p><%= image_tag flat.picture.url(:medium) %></p>
          <%= link_to("More details", flat_path(flat), class: "btn btn-success btn-xs btn-right")%>
          </div>
          <%end%>
      </div>
    </div>
  </div>
</div>
