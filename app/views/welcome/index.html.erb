<div id="accueil">

  <h1>Rent your flat</h1>
  <%= render 'form' %>

  <p>Here are all the flats:</p>
  <ul>
  <%@flats.each do |flat|%>
  <li><%=flat.title%> - Price: <%=flat.price%>€ - City: <%=flat.city%> - <%= link_to("More details", flat_path(flat))%> - <%= link_to("Edit", edit_flat_path(flat))%> - <%= link_to("Delete", flat_path(flat), :method=>:delete)%></li><br>
  <%end%>
  </ul>

</div>